---
version: 1
slug: "src-app-page-tsx"
primary_target: "src/app/page.tsx"
related_targets: ["src/components/landing/landing.tsx","src/components/landing/hero-canvas.tsx"]
---

# Surface brief — Orrery landing (/)

Scope: the marketing front door at `/` (src/app/page.tsx + src/components/landing/*). Mode: Persuade.

Audience: founders/solo devs/small teams burned by overclaiming infra tools. Job: understand what Orrery is, believe the honesty story, click Open Orrery/Begin. Proof: the product demo itself — no invented commercial claims allowed; provider table reads live from the registry; all demo data labeled simulated.

Chosen direction (user-locked, surface roll dealt 3/7/5, pick won): **The Map Assembles** — canvas-2D hero where the atlas system assembles → wires → prices → deploys → goes Live, with a phase-synced narration caption under the CTAs. Sections: one-model (4-surface tab sync), plan-first changeset, the Live moment (working Copy), Navigator plan (periwinkle, approval boundary shown), honesty table (registry-fed), no-lock-in (override-file motif), close.

Memorable moment: the Live chip landing on the canvas while the caption flips to "live at app.atlas.orrery.app — the URL is the finish line".

Constraints: no eyebrow/kicker above headings; mono only for data/code; elevation = hairline only; one authored motion moment (the canvas); reduced-motion renders the final frame; canvas reads theme tokens live (MutationObserver).

Unresolved: mobile visual verification blocked by environment (window won't resize; code-reviewed only); product name "Orrery" is a working name.
