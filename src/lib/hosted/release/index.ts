/**
 * Apps, publish jobs, releases, rollback and suspension.
 *
 * STUB written by the integrator; workstream W7 replaces this file keeping
 * every signature. The job runner is started by `ensureHosted()`.
 */
export function startHostedJobRunner(): void {}

export function stopHostedJobRunner(): void {}
